-- Theme.lua
-- Luxury Hub Theme

local Theme = {}

----------------------------------------------------------------
-- COLORS
----------------------------------------------------------------

Theme.Background = Color3.fromRGB(15, 17, 23)
Theme.Surface = Color3.fromRGB(21, 24, 32)
Theme.Surface2 = Color3.fromRGB(27, 30, 40)
Theme.Card = Color3.fromRGB(32, 36, 48)

Theme.Border = Color3.fromRGB(54, 60, 74)

Theme.Accent = Color3.fromRGB(98, 155, 255)
Theme.AccentDark = Color3.fromRGB(76, 120, 220)

Theme.Green = Color3.fromRGB(92, 220, 140)
Theme.Red = Color3.fromRGB(255, 90, 90)
Theme.Yellow = Color3.fromRGB(255, 200, 70)

Theme.Text = Color3.fromRGB(240,240,245)
Theme.TextDim = Color3.fromRGB(180,185,195)
Theme.TextDark = Color3.fromRGB(125,130,145)

----------------------------------------------------------------
-- CORNERS
----------------------------------------------------------------

Theme.Corner = {
    Window = 18,
    Card = 14,
    Button = 12,
    Toggle = 999,
    Input = 10,
    Tab = 12,
}

----------------------------------------------------------------
-- STROKES
----------------------------------------------------------------

Theme.Stroke = {
    Normal = 1,
    Selected = 2,
}

----------------------------------------------------------------
-- SHADOW
----------------------------------------------------------------

Theme.Shadow = {
    Transparency = 0.55,
    Size = 10,
}

----------------------------------------------------------------
-- PADDING
----------------------------------------------------------------

Theme.Padding = {
    Window = 18,
    Card = 12,
    Header = 18,
}

----------------------------------------------------------------
-- ANIMATION
----------------------------------------------------------------

Theme.Animation = {
    Fast = 0.12,
    Normal = 0.18,
    Slow = 0.28,
}

----------------------------------------------------------------
-- FONTS
----------------------------------------------------------------

Theme.Font = Enum.Font.Gotham

Theme.FontBold = Enum.Font.GothamBold

Theme.FontSemibold = Enum.Font.GothamMedium

----------------------------------------------------------------
-- TEXT SIZES
----------------------------------------------------------------

Theme.TextSize = {
    Title = 22,
    Header = 18,
    Normal = 15,
    Small = 13,
    Tiny = 11,
}

----------------------------------------------------------------
-- TOGGLE
----------------------------------------------------------------

Theme.Toggle = {
    Width = 46,
    Height = 24,
    Knob = 18,
}

----------------------------------------------------------------
-- BUTTON
----------------------------------------------------------------

Theme.Button = {
    Height = 36,
}

----------------------------------------------------------------
-- TAB
----------------------------------------------------------------

Theme.Tab = {
    Width = 120,
    Height = 42,
}

----------------------------------------------------------------
-- WINDOW
----------------------------------------------------------------

Theme.Window = {
    Width = 640,
    Height = 430,
    MinWidth = 500,
    MinHeight = 340,
}

----------------------------------------------------------------
-- LIST
----------------------------------------------------------------

Theme.List = {
    Spacing = 10,
}

----------------------------------------------------------------
-- ICONS
----------------------------------------------------------------

Theme.IconColor = Theme.TextDim
Theme.IconSelected = Theme.Accent

----------------------------------------------------------------
-- EFFECTS
----------------------------------------------------------------

Theme.HoverTransparency = 0.15
Theme.ClickTransparency = 0.25

return Theme
